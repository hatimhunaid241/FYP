# ── Core data science ────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.11

# ── Machine learning ─────────────────────────────────────────────────────
scikit-learn>=1.3,<2.0

# ── Deep learning & embeddings ───────────────────────────────────────────
# torch: install the correct variant for your hardware from https://pytorch.org
# CPU-only (works everywhere, slower):
torch>=2.0
# GPU (CUDA 11.8) — uncomment and comment the line above:
# torch>=2.0+cu118
sentence-transformers>=2.2,<3.0

# ── Chinese text processing ──────────────────────────────────────────────
jieba>=0.42
opencc-python-reimplemented>=0.1.7   # import as: from opencc import OpenCC

# ── Dimensionality reduction (for embedding visualizations) ──────────────
umap-learn>=0.5

# ── Visualization ────────────────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# ── API & data collection ────────────────────────────────────────────────
requests>=2.31
tqdm>=4.65

# ── Configuration & utilities ────────────────────────────────────────────
PyYAML>=6.0
python-dotenv>=1.0

# ── Testing ──────────────────────────────────────────────────────────────
pytest>=7.0

# ── Jupyter (optional — for notebooks) ──────────────────────────────────
jupyter>=1.0
ipywidgets>=8.0

# ── Parquet support (required by pandas.read_parquet) ───────────────────
pyarrow>=12.0

# ── Optional: faster FAISS search for very large datasets ───────────────
# faiss-cpu>=1.7     # CPU version
# faiss-gpu>=1.7     # GPU version (requires CUDA)
